<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alseulsanjap">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".di.SsagApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_ssag"
        android:label="@string/ssag"
        android:roundIcon="@mipmap/ic_ssag"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alseulsanjap"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.CheckResultActivity"
            android:exported="false" />
        <activity
            android:name=".ui.CertificationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>